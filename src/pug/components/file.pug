mixin file(format = 'PDF', size = '99.9KB', props = {})
    -
        var {
            tag = 'div',
        } = props

    #{tag}.file&attributes(attributes)
        i.icon-file(class='icon-file')
        .file__content
            span.file__text File
            span.file__size= `${format}, ${size}`