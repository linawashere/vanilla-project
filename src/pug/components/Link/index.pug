mixin link(props = {})
    -
        var {
            tag = 'a',
            type = null,
            size = 's',
        } = props

    #{tag}.link(
        class=[
            `link--${type}`,
            `link--${size}`
        ]
    )&attributes(attributes)
        i.icon
        span.link__text
            block