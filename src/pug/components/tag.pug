mixin tag(props = {})
    -
        var {
            tag = 'button',
            type = '',
        } = props

    #{tag}.tag(class=`tag--${type} tag--${color}`)&attributes(attributes)
        .tag__inner
            span.tag__text
                block