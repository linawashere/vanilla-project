mixin tag(props = {})
    -
        var {
            tag = 'button',
            type = '',
            color = ''
        } = props

    #{tag}.tag(class=`tag__${type} tag__${color}`)&attributes(attributes)
        .tag__inner
            span.tag__text
                block