mixin actionButton(props={})
    -
        var {
            tag = 'button',
            type = 'bookmark',
        } = props

    #{tag}.action-button(
        class=type
    )&attributes(attributes)
        i.icon(class=`icon-${type}`)